# Frontend Implementation Prompt

Build a mobile-first Next.js App Router feature for the Tamil Nadu election contest.

Constraints:

- Use existing components in `components/`.
- Keep data access inside `lib/data/adapter.ts` or a small feature-specific helper.
- Do not put Supabase SQL directly in UI components.
- Preserve fixture fallback behavior when Supabase env vars are absent.
- Add at least one test or manual checklist item for risky changes.
